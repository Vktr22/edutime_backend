<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class AuthController extends Controller
{
    
    /**
         * This controller will handle API authentication
         * using Sanctum personal access tokens (Bearer tokens).
         *
         * Step-by-step we will add:
         *  - login()  → validate credentials, create token, return user+token
         *
    */
}
